#include <stdio.h>

int main(void) {
    int m, n, current;
    int map[500][500];

    scanf("%d %d", &m, &n);

    for (int i = 0; i < m; i++)
    {
        for (int j = 0; j < n; j++)
        {
            scanf("%d", &map[m][n]);
        }
    }
    
    
}